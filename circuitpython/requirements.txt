asyncio
adafruit_wave
adafruit_displayio_sh1106
adafruit_display_text
adafruit_mpr121
neopixel
tmidi

# pulled in by lib/relic_synthiota.py for its buttons
adafruit_debouncer

# github.com/todbot/CircuitPython_SynthTools -- the synth engine.
# NOTE: needs a version newer than the current bundle release (0.5.1),
# which lacks the wavetable LFO and synth.update() and has a broken
# update_knobs_scale. Until a newer release lands, the needed .py source
# is copied into lib/synthtools/ from a checkout of the repo; delete that
# folder once `circup install synthtools` gives a new enough version.
synthtools

# lib/ modules copied to the device (not circup packages):
#   relic_synthiota.py      -- github.com/relic-se/CircuitPython_Synthiota
#   synthiota_potpage_ui.py -- the 8-pot two-up param screen
#   harmony.py              -- scales + chords; headed for synthtools.harmony
#   synthtools/*.py         -- see the note above
lib/synthiota_potpage_ui.py
lib/relic_synthiota.py
lib/harmony.py
